MISMATCH: this commit message describes the mic-device-recovery fix, but the diff only adds HANDOFF_CODEX_MIC_DEVICE_RECOVERY.md (no source changes). The actual implementation (MicrophoneCaptureService.swift + tests) is in the NEXT commit, 4301ecb3, whose own message says 'chore: untrack HANDOFF doc' — misleading in the same way. Confirmed 2026-07-24; documented non-destructively (no rewrite) per user decision.
